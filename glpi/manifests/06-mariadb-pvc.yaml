apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oci-block-volume-mariadb-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi  # Tamanho do PVC que será requisitado
  storageClassName: oci-bv